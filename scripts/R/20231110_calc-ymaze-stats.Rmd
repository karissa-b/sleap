---
title: "Y-maze analysis"
author: "AA"
date: "2023-11-09"
output: html_document
editor_options:
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

knit2 <- function(filename = rstudioapi::getSourceEditorContext()$path, output_dir = tempdir()) {
  result <- rmarkdown::render(filename, output_dir = output_dir)
  getOption("viewer")(result)
}
```

```{r}
data <- read_rds("processed_data/lin_interp_data.rds")
```

```{r}
source("scripts/ymaze-stats.R")

ymaze_data <- calc_ymaze_stats(data)
```

I now need to fit models.

```{r}
ymaze
```

